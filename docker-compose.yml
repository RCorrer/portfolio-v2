version: '3.8'
services:
  dev:
    build: .
    ports:
      - "8080:80"      # Mapeia a porta 80 do container para a 8080 do seu PC
    volumes:
      - .:/usr/share/nginx/html  # Bind mount para hot-reload